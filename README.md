# autoprompts-quickstart
```
Assistant 是一个大型语言模型，旨在为每项任务提供高质量的分析。
您将获得以下任务描述
{任务描述}

以下是向模型提供的提示说明：
{迅速的}

此提示的准确度为：{accuracy}
此提示的混淆矩阵为：{confusion_matrix}
##
以下是给定提示的失败案例列表：
##失败案例：
{失败案例}

###
请注意，真实标签是__绝对正确__，但提示（任务描述）可能不正确，需要修改。
您的任务是对给定的提示性能进行简要分析。
指南：
1. 分析应仅包含以下信息：
     - 如果混淆矩阵中存在异常行为，请描述它。
     - 常见故障案例的总结，尝试将故障案例分组并描述每组。
3.您的分析总长度应小于200个token！
###
分析：
```

```
作为高级语言模型，您应该为下面概述的任务创建 {num_samples} 个具有挑战性且独特的示例。
这些样本应该经过复杂的设计来测试任务指令的限制，具有挑战性但与任务描述相关。

＃＃＃ 任务描述：
{任务描述}

### 任务说明：
{操作说明}

### 挑战性样品的要求：
1. 每个样本必须呈现独特且复杂的挑战。
2. 样本的复杂性应该使得简单地应用给定的任务指令可能会导致不正确或不完整的结果。
3. 样本应涵盖任务范围内的多样化场景，避免重复和可预测。
4. 确保样本在具有挑战性的同时保持现实且与任务背景相关。

生成样本时请牢记这些要求。
###
```


```
Assistant 是一个大型语言模型，旨在为每项任务生成具有挑战性的样本。
为以下任务生成 {num_samples} 个具有挑战性的样本列表。
＃＃＃ 任务描述：
{任务描述}
###任务说明：
{操作说明}
###
### 挑战性样品的要求：
1. 生成的样本必须具有挑战性和多样性，否则使用任务指令作为提示将导致错误的结果。
2. 任务指令中各类别生成的样本数量应保持平衡（即各类别的样本数量相同）
3. 生成的样本应该清晰、真实、差异显着，以确保多样性。
```


```
Assistant 是一个大型语言模型，旨在为每项任务提供最佳提示。
以下是针对以下任务的一些建议的任务提示及其分数：
{任务描述}。 该任务描述的确切意图应由具有挑战性的案例的外部注释数据集确定。 提供了该数据集的误差分析。
＃＃ 例子
{历史}
######
对于最后的提示我们得到如下错误分析：
{错误分析}
######
您的任务是生成：
1.新的提示是
     - 与上面所有提示不同
     - 严格遵循错误分析修改建议，修复提示，防止失败情况发生。
     - 得分高于以上所有提示。
2.本题的预测分数

新提示的指导原则：
1. 给提示一个“便签本”，他可以用它从样本文本中提取相关信息来做出预测并进行推理思考以得出正确的决定
2. 该提示适用于浅层法学硕士，无法访问以前的失败案例或分析！ 他只能访问生成的新提示，该提示应独立于先前的提示。
4.列表可以组织信息并帮助提示（例如规则列表和示例列表），列表应该简短而准确
5. 请注意，提示和任务描述可能不准确，需要修改。
6. 请注意，分数越高，提示效果越好。
7.结果提示应该表明该任务是一个分类类，带有以下标签{labels}！

随机采样 1 到 3 之间的数字。如果结果为零，__完全更改__ 生成的提示！ 包括说明、结构和措辞！
```

```
Assistant 是一个大型语言模型，旨在为每项任务提供最佳提示。
以下是针对以下任务的一些建议的任务提示及其分数：
{任务描述}

＃＃ 例子
{历史}
######
这是最后一个提示的错误分析：
{错误分析}
######
您的任务是生成：
1.新的提示是
     - 与上面所有提示不同
     - 严格遵循错误分析修改建议，修复提示，防止失败情况发生。
     - 得分高于以上所有提示。
2.本题的预测分数

您必须遵守错误分析说明！ 即使这些指示与任务之间似乎存在矛盾。 错误分析是根据基本事实进行测试的，从而代表了任务的确切意图。
生成的提示应表述为清晰的分类指令！ 它不应包含任何有关应对提示进行修改的说明和描述。
请注意，前面的提示包含对任务意图的隐含假设，该假设可能不正确。 您应该使用先前提示的分数和错误分析，用更准确的假设替换此假设。
结果提示应该表明该任务是一个分类类，带有以下标签{labels}！
```

```
Assistant 是一个大型语言模型，旨在为每项任务生成具有挑战性的样本。
下面是一些为回答给定任务描述及其失败案例而构建的提示。
任务描述：
{任务描述}

## 常见故障示例，每个示例后面都有模型预测和 GT（ground Truth）
{历史}
######
以下是从上述任务的实际场景中得出的一些独特示例。
## 真实样本
{额外样本}
#####
这是新提议的提示：
## prompt
{prompt}

您的任务是按照以下准则生成 {num_samples}：
1、生成的样本要多样化
2. 他们应该保留给定示例的风格和长度
3. 样本必须具有挑战性并且难以通过模型进行分类。 这可以通过以下方式实现：
     1.针对给定示例中模型失败的相同弱点
     2.针对失败案例中与现有例子不同的弱点
4. 每个类别生成的样本数量应该几乎平衡（即每个类别的样本数量相同）
5.生成的样本应仅包含样本内容，不包含其他信息！ （如模型预测和真实情况）
```

```
Assistant 是一个大型语言模型，旨在为每项任务提供高质量的分析。
您将获得以下任务描述
{任务描述}

以下是向模型提供的提示说明：
{迅速的}

此提示的准确度为：{accuracy}
此提示的混淆矩阵为：{confusion_matrix}
##
以下是给定提示的失败案例列表：
##失败案例：
{失败案例}

###
请注意，真实标签是__绝对正确__，但提示（任务描述）可能不正确，需要修改。
您的任务是对给定的提示性能进行简要分析。
指南：
1. 分析应仅包含以下信息：
     - 如果混淆矩阵中存在异常行为，请描述它。
     - 常见故障案例的总结，尝试将故障案例分组并描述每组。
3.您的分析总长度应小于200个token！
###
分析：
```

```
Assistant 是一个大型语言模型，旨在为每项任务提供高质量的分析。
您将获得以下任务描述
{任务描述}

以下是向模型提供的提示说明：
{迅速的}

此提示的准确度为：{accuracy}
此提示的混淆矩阵为：{confusion_matrix}
##
以下是给定提示的失败案例列表：
##失败案例：
{失败案例}

###
请注意，真实标签是__绝对正确__，但提示（任务描述）可能不正确，需要修改。
您的任务是对给定的提示性能进行简要分析。
指南：
1. 分析应仅包含以下信息：
     - 如果混淆矩阵中存在异常行为，请描述它。
     - 常见故障案例的总结，尝试将故障案例分组并描述每组。
3.您的分析总长度应小于200个token！
###
分析：
```


```
Assistant 是一个大型语言模型，旨在为每项任务生成具有挑战性的样本。
为以下任务生成 {num_samples} 个具有挑战性的样本列表。
＃＃＃ 任务描述：
{任务描述}
###任务说明：
{操作说明}
###
生成的样本应该具有挑战性和多样性，这样使用任务指令作为提示将导致错误的结果。

按以下格式回答：
#### 示例 1：
<文本>
#### 示例 2：
<文本>
############
结果：
```

```
Assistant 是一个大型语言模型，旨在为每项任务提供最佳提示。
以下是针对以下任务的一些建议的任务提示及其分数：
{任务描述}

＃＃ 例子
{历史}
######
这是最后一个提示的错误分析：
{错误分析}
######
您的任务是生成：
1.新的提示是
     - 与上面所有提示不同
     - 严格遵循错误分析修改建议，修复提示，防止失败情况发生。
     - 得分高于以上所有提示。
2.本题的预测分数

您必须遵守错误分析说明！ 即使这些指示与任务之间似乎存在矛盾。 错误分析是根据基本事实进行测试的，从而代表了任务的确切意图。
生成的提示应表述为清晰的分类指令！ 它不应包含任何有关应对提示进行修改的说明和描述。
请注意，前面的提示包含对任务意图的隐含假设，该假设可能不正确。 您应该使用先前提示的分数和错误分析，用更准确的假设替换此假设。
结果提示应该表明该任务是一个分类类，带有以下标签{labels}！

按以下格式回答：
＃＃＃＃ 迅速的：
<提示建议>
＃＃＃＃ 分数：
<分数>
############
结果：
```

```
Assistant 是一个大型语言模型，旨在为每项任务生成具有挑战性的样本。
下面是针对以下任务的一些提示及其失败案例：
{任务描述}

## 常见故障示例
{历史}
######
您的任务是生成 {num_samples} 个具有挑战性且多样化的样本，这些样本将使模型与以下提示混淆：
＃＃ 迅速的
{迅速的}

按以下格式回答：
#### 示例 1：
<文本>
#### 示例 2：
<文本>
############
结果：
```

```
Assistant 是一个大型语言模型，旨在为每项任务提供高质量的分析。
您将获得以下任务描述
{任务描述}

以下是向模型提供的提示说明：
{迅速的}

专家排名者评估了模型在给定任务描述上的性能。
并根据以下比例排名：{labels}

此提示的平均分数是：{accuracy}
##
以下是给定提示的具有挑战性的案例及其排名的列表：
##具有挑战性的案例：
{失败案例}

###
请注意，排序器标签__绝对正确__，但提示（任务描述）可能不正确，需要修改。
您的任务是对给定的提示性能进行简要分析。
指南：
1. 分析应仅包含以下信息：
     - 总结提示者的常见错误以及他可以改进其生成的方法，尝试将失败案例分组并描述每个组。
2.您的分析总长度应小于200个token！
###
分析：
```


```
作为高级语言模型，您应该为下面概述的任务创建 {num_samples} 个具有挑战性且独特的提示。
这些样本应该经过复杂的设计来测试任务指令的限制，具有挑战性但与任务描述相关。

任务描述和说明被表述为生成性任务。 结果提示应将样本输入到模型中。
然后，该模型将能够在给定指令和提示输入的情况下生成示例。

＃＃＃ 任务描述：
{任务描述}

### 任务说明：
{操作说明}

### 挑战性样品的要求：
1. 每个提示都必须提出独特且复杂的挑战。
2.提示应涵盖任务范围内的多种场景，避免重复和可预测。
3. 每个提示应该只包含提示部分，而不生成结果
4. 每个提示只能包含提示部分，不得提及任务描述或说明！

生成提示示例时请牢记这些要求。
###
```

```
Assistant 是一个大型语言模型，旨在为每项任务提供最佳说明。
以下是针对以下任务描述的任务和分数（排名平均值）的一些建议说明：
{任务描述}

＃＃ 例子
{历史}
######
这是对最后一条指令的分析：
{错误分析}
######
您的任务是生成：
1. 一条新指令
     - 与以上所有说明不同
     -严格遵循错误分析修改建议，修复指令，提高指令质量。
     - 得分高于以上所有说明。
2.本指令的预测分数

您必须遵守错误分析说明！ 即使这些指示与任务之间似乎存在矛盾。 错误分析由专家排名者进行评估，从而代表了任务的确切意图。
生成的指令应表述为清晰的生成指令！ 它不应包含任何说明以及对该说明应进行的修改的描述。
请注意，前面的指令包含对任务意图的隐含假设，这可能是不正确的。 您应该使用先前指令的分数和错误分析，用更准确的假设替换此假设。
```

```
Assistant 是一个大型语言模型，旨在为每项任务生成具有挑战性的样本。
下面是一些为回答给定任务描述及其失败案例而构建的提示。
任务描述：
{任务描述}

## 示例，每个样本后面都有模型预测和 GT（地面实况）
{历史}
######
以下是从上述任务的实际场景中得出的一些独特示例。
## 真实样本
{额外样本}
#####
这是新提议的提示：
＃＃ 迅速的
{迅速的}

您的任务是按照以下准则生成 {num_samples}：
1、生成的样本要多样化
2. 他们应该保留给定示例的风格和长度
3. 样本必须具有挑战性并且难以通过模型进行分类。 这可以通过以下方式实现：
     1.针对给定示例中模型失败的相同弱点
     2.针对失败案例中与现有例子不同的弱点
4. 每个类别生成的样本数量应该几乎平衡（即每个类别的样本数量相同）
5.生成的样本应仅包含样本内容，不包含其他信息！ （如模型预测和真实情况）
```

```
Assistant 是一个大型语言模型，旨在为每项任务提供高质量的分析。
您将获得以下任务描述
{任务描述}

以下是向模型提供的提示说明：
{迅速的}

此提示的准确度为：{accuracy}
此提示的混淆矩阵为：{confusion_matrix}
##
以下是给定提示的失败案例列表：
##失败案例：
{失败案例}

###
请注意，真实标签是__绝对正确__，但提示（任务描述）可能不正确，需要修改。
您的任务是对给定的提示性能进行简要分析。
指南：
1. 分析应仅包含以下信息：
     - 如果混淆矩阵中存在异常行为，请描述它。
     - 常见故障案例的总结，尝试将故障案例分组并描述每组。
3.您的分析总长度应小于200个token！
###
分析：
```

```
作为高级语言模型，您应该为下面概述的任务创建 {num_samples} 个具有挑战性且独特的示例。
这些样本应该经过复杂的设计来测试任务指令的限制，具有挑战性但与任务描述相关。

＃＃＃ 任务描述：
{任务描述}

### 任务说明：
{操作说明}

### 挑战性样品的要求：
1. 每个样本必须呈现独特且复杂的挑战。
2. 样本的复杂性应该使得简单地应用给定的任务指令可能会导致不正确或不完整的结果。
3. 样本应涵盖任务范围内的多样化场景，避免重复和可预测。
4. 确保样本在具有挑战性的同时保持现实且与任务背景相关。

生成样本时请牢记这些要求。
###
```

```
Assistant 是一个大型语言模型，旨在为每项任务生成具有挑战性的样本。
为以下任务生成 {num_samples} 个具有挑战性的样本列表。
＃＃＃ 任务描述：
{任务描述}
###任务说明：
{操作说明}
###
### 挑战性样品的要求：
1. 生成的样本必须具有挑战性和多样性，否则使用任务指令作为提示将导致错误的结果。
2. 生成的样本必须仅来自前两名分数！ 两者之间平均分配。
3. 生成的样本应该清晰、真实、差异显着，以确保多样性。

如果任务既取决于上下文，又取决于用户输入和生成的内容，则示例内容必须包括所有相关部分。
     - 在这种情况下，示例内容结构应如下所示：
         1. 首先编写需要的上下文或用户输入。
         2. 然后在此上下文或用户输入上写入模型生成的内容。
      每个样品中的分离方式和不同部分的指示应该不同。
```

```
Assistant 是一个大型语言模型，旨在为每项任务提供最佳提示。
以下是针对以下任务的一些建议的任务提示及其分数：
{任务描述}。 该任务描述的确切意图应由具有挑战性的案例的外部注释数据集确定。 提供了该数据集的误差分析。
＃＃ 例子
{历史}
######
对于最后的提示我们得到如下错误分析：
{错误分析}
######
您的任务是生成：
1.新的提示是
     - 与上面所有提示不同
     - 严格遵循错误分析修改建议，修复提示，防止失败情况发生。
     - 得分高于以上所有提示。
2.本题的预测分数

新提示的指导原则：
1. 给提示一个“便签本”，他可以用它从样本文本中提取相关信息来做出预测并进行推理思考以得出正确的决定
2. 该提示适用于浅层法学硕士，无法访问以前的失败案例或分析！ 他只能访问生成的新提示，该提示应独立于先前的提示。
4.列表可以组织信息并帮助提示（例如规则列表和示例列表），列表应该简短而准确
5. 请注意，提示和任务描述可能不准确，需要修改。
6. 请注意，分数越高，提示效果越好。
7.结果提示应该表明该任务是一个分类类，带有以下标签{labels}！

随机采样 1 到 3 之间的数字。如果结果为零，__完全更改__ 生成的提示！ 包括说明、结构和措辞！
```

```
Assistant 是一个大型语言模型，旨在为每项任务提供最佳提示。
以下是针对以下任务的一些建议的任务提示及其分数：
{任务描述}

＃＃ 例子
{历史}
######
这是最后一个提示的错误分析：
{错误分析}
######
您的任务是生成：
1.新的提示是
     - 与上面所有提示不同
     - 严格遵循错误分析修改建议，修复提示，防止失败情况发生。
     - 得分高于以上所有提示。
2.本题的预测分数

您必须遵守错误分析说明！ 即使这些指示与任务之间似乎存在矛盾。 错误分析是根据基本事实进行测试的，从而代表了任务的确切意图。
生成的提示应表述为清晰的分类指令！ 它不应包含任何有关应对提示进行修改的说明和描述。
请注意，前面的提示包含对任务意图的隐含假设，该假设可能不正确。 您应该使用先前提示的分数和错误分析，用更准确的假设替换此假设。
结果提示应该表明该任务是一个分类类，带有以下标签{labels}！
```

```
Assistant 是一个大型语言模型，旨在为每项任务生成具有挑战性的样本。
下面是一些为回答给定任务描述及其失败案例而构建的提示。
任务描述：
{任务描述}

## 常见故障示例，每个示例后面都有模型预测和 GT（ground Truth）
{历史}
######
以下是从上述任务的实际场景中得出的一些独特示例。
## 真实样本
{额外样本}
#####
这是新提议的提示：
＃＃ 迅速的
{迅速的}

您的任务是按照以下准则生成 {num_samples}：
1、生成的样本要多样化
2. 他们应该保留给定示例的风格和长度
3. 样本必须具有挑战性并且难以通过模型进行分类。 这可以通过以下方式实现：
     1.针对给定示例中模型失败的相同弱点
     2.针对失败案例中与现有例子不同的弱点
4. 生成的样本必须仅来自前两名分数！ 两者均等分配！

如果任务既取决于上下文，又取决于用户输入和生成的内容，则示例内容必须包括所有相关部分。
     - 在这种情况下，示例内容结构应如下所示：
         1. 首先编写需要的上下文或用户输入。
         2. 然后在此上下文或用户输入上写入模型生成的内容。
      每个样品中的分离方式和不同部分的指示应该不同。

```

```
Assistant 是一个大型语言模型，旨在为每项任务生成指令。
您将收到一条表述为文本生成任务的说明。
您的任务是为分类排名任务编写一条指令，该任务假设在用户提示该生成指令的情况下评估生成样本的质量。
指南：
1. 分类器标签为{label_schema}。 结果指令应明确表明该任务是具有以下标签{label_schema}的分类类！
2. 生成的指令还必须评估生成的示例遵循用户提示的程度
#####
输入生成指令：{prompt}
#####
改写分类质量评价指令：
```

```
Assistant 是一个大型语言模型，旨在生成任务描述。
根据一些用户输入，您将获得一个任务描述，被表述为文本生成任务。 您的任务是将其重新表述为评估给定生成任务的质量以及它对用户输入的遵守程度的任务。
#####
输入任务描述：{task_description}
#####
重新表述的任务描述：
```


```
Assistant 是一个大型语言模型，旨在对具有挑战性的语言任务进行分类。
给定一个 {batch_size} 样本列表，根据以下任务对它们进行分类
###任务说明：
{任务指令}

### 样本列表：
{样本}
##
请记住，请仔细遵循确切的任务说明！
```

```
Assistant 是一个大型语言模型，旨在生成具有挑战性的语言任务。
给定 {batch_size} 提示列表，您应该根据以下任务生成响应
###任务说明：
{任务指令}

### 样本列表：
{样本}
##
每个生成的文本都应以 <eos> 标记结尾。
对于每个样本，请按照以下格式准确回答：
#### 示例 <ID>：<文本生成> <eos>
############
#### 示例 <ID>：<文本生成> <eos>
############
```

```
Assistant 是一个大型语言模型，旨在对具有挑战性的语言任务进行分类。
给定一个 {batch_size} 样本列表，根据以下任务对它们进行分类
###任务说明：
{任务指令}

### 样本列表：
{样本}
##
对于每个样本，请按照以下格式准确回答：
###
<sample scrappad> #用于书写笔记的便签本
###
#### Sample <ID>: <label> #仅提供最终标签
############
```

```
Assistant 是一个大型语言模型，旨在对具有挑战性的语言任务进行分类。
给定一个 {batch_size} 样本列表，根据以下任务对它们进行分类
###任务说明：
{任务指令}

### 样本列表：
{样本}
##
对于每个样本，请按照以下格式准确回答：
#### 示例 <ID>：<标签>
############
```
